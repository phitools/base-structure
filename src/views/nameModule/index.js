import React from "react";
import { Container, Typography } from "@material-ui/core";

const NameModule = () => {
  return (
    <Container>
      <Typography variant="h3">Base structure App</Typography>
    </Container>
  );
};

export default NameModule;
